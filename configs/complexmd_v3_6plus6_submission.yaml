seed: 42

# Only history_frames is consumed by the public-data inference adapter. The
# training-data paths are deliberately absent from the reproducibility package.
data:
  history_frames: 12
  pocket_cutoff: 12.0
  max_pocket_residues: 128

model:
  generation_method: hierarchical_pose_rigid_fragment_flow
  hidden_dim: 128
  num_heads: 4
  num_layers: 6
  num_rbf: 32
  rbf_max: 20.0
  dropout: 0.1
  diffusion_steps: 100
  diffusion_schedule: linear
  max_displacement: 5.0
  max_atomic_number: 118
  flow_base_scale: 1.0
  flow_time_scale: 1000.0
  flow_solver: heun
  translation_base_scale: 0.10
  rotation_base_scale: 0.05
  torsion_base_scale: 0.0
  torsion_target_scale: 1.0
  torsion_step_limit_deg: 5.0
  translation_step_limit: 0.5
  rotation_step_limit_deg: 5.0
  endpoint_loss_weight: 0.10
  projection_regularization: 0.0001
  fragment_velocity_scale: 1.0
  current_window_frames: 6
  historical_window_frames: 6
  torsion_confidence_threshold: 0.75
  torsion_confidence_target_deg: 5.0
  torsion_confidence_loss_weight: 0.10
  pose_max_translation: 5.0
  pose_max_rotation_deg: 15.0
  pose_loss_weight: 1.0
  ligand_loss_weight: 0.25
  pose_rotation_loss_weight: 25.0

evaluation:
  sampling_steps: 10
  pose_translation_scale: 0.25
  pose_rotation_scale: 0.25

